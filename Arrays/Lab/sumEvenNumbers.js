function sumEvenNumber(input) {

    let sum = 0;
    let arrayLength = input.length;
    for (number of input) {
        if (+number % 2 === 0) {
            sum += +number
        }
    }
    console.log(sum)
}
sumEvenNumber(['1', '2', '3', '4', '5', '6'])