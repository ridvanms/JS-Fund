function solve(name, age, grade) {
    let studentName = name
    let studentAge = age;
    let studentGrade = grade.toFixed(2);

    console.log(`Name: ${name}, Age: ${studentAge}, Grade: ${studentGrade}`)
}
solve('John', 15, 5.45643)